apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yet-another-aws-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "yet-another-aws-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yet-another-aws-exporter.fullname" . }}:9100']
  restartPolicy: Never
